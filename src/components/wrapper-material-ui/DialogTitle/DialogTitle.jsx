import React from 'react';
import { default as WDialogTitle } from '@material-ui/core/DialogTitle';

function DialogTitle(props) {
    return <WDialogTitle {...props} />;
}

export default DialogTitle;
